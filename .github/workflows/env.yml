name: Env Demo

on:
  push:

env:
  APP_ENV: "dev"

jobs:
  env_demo:
    runs-on: ubuntu-latest

    steps:
      - name: Connecting to Env
        run: echo "App is connecting to $APP_ENV"